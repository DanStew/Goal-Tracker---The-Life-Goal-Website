import DefaultWebPage from "../Components/DefaultWebPage";

function Timetable({ colourScheme }) {
  return (<DefaultWebPage colourScheme={colourScheme} webpage={"Timetable"}/>);
}

export default Timetable;
